# SaaS & AI Tool Selection Guide — Image Instructions for Illustration AI

Hard rules for this set:
- All final images must be **100% English, zero Chinese characters** (Chinese and English article versions share the same image set).
- Every figure must be **100% accurate text/labels**, never AI-generated. Use **HTML chart / HTML-to-screenshot**, not AI, for any diagram.
- **No fabricated statistics.** No invented prices, percentages, or ratings.
- **Image-content uniqueness (per site rule):** the article body already enumerates the 3 pipelines and the 4-item pre-subscribe checklist as numbered lists. Those are NOT redrawn as images (that would duplicate the text). Only one schematic is kept — a relation the prose states but never draws.

---

## 01-gap.png  (the ONLY figure for this article)

- **Filename:** `imgs/01-gap.png`
- **Why it exists:** the opening says "we paid for six tools, used two" — a *concept*, not a drawn shape. This figure makes the waste gap visible. It is a **relation/comparison diagram**, NOT a redraw of any list in the body.
- **Type:** HTML chart (subset / Venn-style diagram)
- **Login wall:** no
- **Language:** 100% English, zero Chinese
- **Content (element-level):**
  - A large outer ring labeled **"Subscriptions you pay for"**.
  - A smaller inner ring inside it labeled **"Tools you open every week"**.
  - The area between the two rings is shaded and labeled **"Idle spend"** (this is the wasted gap).
  - No numbers anywhere (do NOT put "6 vs 2" — that is already in the prose; the diagram only shows the gap shape).
- **Note:** this is the single permitted figure. The 3-pipeline breakdown and the 4-item checklist live as text in the body and must NOT be illustrated as separate images.

---

## 03 — Scenario decision logic (NO IMAGE — compiles into a dynamic Quiz)

- **Do NOT generate an image.** The scenario decision logic (Scenario A–D, need → tool map) is written as structured text in the body and is compiled by the user's web tool into an interactive Quiz.
- **Logic summary (for the Quiz builder):**
  - Interactive demos / training / live feedback, no enterprise plan → lightweight live-polling tool (e.g. AhaSlides).
  - Cold email that must land in the inbox → deliverability / warm-up tool (e.g. WarmupInbox).
  - B2B bulk verified leads → lead finder + verification (e.g. Snov.io).
  - Video / YouTube AI scripts → script tool by cadence (batch vs retention-focused).
- **Language:** 100% English, zero Chinese (Quiz UI). No fabricated numbers.

---

## Removed (were redundant — do NOT regenerate)

- `01-mvs.png` — redrew the 3 pipelines already listed in "Step 1". Deleted.
- `02-pitfalls.png` — redrew the 4-item pre-subscribe checklist already listed in "Step 2". Deleted.
