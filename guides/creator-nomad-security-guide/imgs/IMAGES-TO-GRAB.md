# Creator & Digital Nomad Security Guide — Image Instructions for Illustration AI

Hard rules for this set:
- All final images must be **100% English, zero Chinese characters** (Chinese and English article versions share the same image set).
- Every figure must be **100% accurate text/labels**, never AI-generated. Use **HTML chart / HTML-to-screenshot**, not AI.
- **No fabricated statistics.** No invented percentages or threat numbers.
- **Image-content uniqueness (per site rule):** the body already enumerates the 3-2-1 rule and the 7-item self-check list. Those are NOT redrawn as images. Only one figure is kept — the layered-defense hierarchy, which shows a *stacking + threat-blocking relation* the prose describes but never draws as a diagram.

---

## 01-layers.png  (the ONLY figure for this article)

- **Filename:** `imgs/01-layers.png`
- **Why it exists:** "layered defense" is the article's core metaphor. The body explains each layer in words; this figure shows the **visual structure** — three shields stacked, each stopping one threat. It uses icons + very short labels only; it does NOT re-explain the layer contents (that would duplicate the prose).
- **Type:** HTML chart (hierarchy diagram)
- **Login wall:** no
- **Language:** 100% English, zero Chinese
- **Content (element-level):**
  - Three stacked horizontal shield layers, top to bottom:
    1. **Identity & Passwords** (lock icon)
    2. **Network & Privacy** (shield-with-wifi icon)
    3. **Data & Backup** (disk icon)
  - On the right, three small threat icons with short labels, each pointing to the layer that stops it:
    - Stolen account → Layer 1
    - Sniffed session → Layer 2
    - Lost data → Layer 3
  - No sentences, no definitions — only icons + the short labels above.

---

## Removed (were redundant — do NOT regenerate)

- `02-321.png` — redrew the 3-2-1 backup rule already listed in "Layer 3". Deleted.
- `03-checklist.png` — redrew the 7-item self-check list already listed in "The Security Self-Check List". Deleted.

---

## Optional atmosphere background (fallback only, textless)

- Dark navy (#0f172a), three glowing shield layers stacked with key/lock icons, teal-to-blue gradient, no text. Only if the HTML chart cannot be produced.
